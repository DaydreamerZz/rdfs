接下来任务:
1. client收到agent的目录树结构后,发送文件不再是简单的上传了.client首先会检查目录树结构
   如果已有,会发送失败.这个和HDFS一样的处理方式;
   如果没有,开始发送.发送完毕之后会给agent一个消息.agent此时会更新自己的目录树结构

2. agent不会每次都把目录树发送给client.第一次发送之后,agent会设置自己的标志位.也就是每次只有新的client连接时会收到目录树.之后,只有执行了uplaod指令,agent会修改标志位.
但是这样处理真的很繁琐.如果放在agent校验呢?
这个先不管了,每次都把目录树给client好了.

3. 删除的功能怎么实现呢?
    想法是采用append log的方式,每隔一段时间把日志发送给所有的storage server,让storage server自己删除文件.但是客户端只要收到更新的目录树,就会认为文件已经被删除了.